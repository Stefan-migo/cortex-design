{
  "$schema": "https://opencode.ai/config.json",
  "mcp": {
    "graphify": {
      "type": "local",
      "enabled": true,
      "command": [
        "graphify",
        "mcp",
        "--project-dir",
        "/run/media/stefan/Nuevo vol/Proyectos/CortexDesign"
      ],
      "_description": "Graphify — knowledge graph queries via MCP"
    },
    "webfetch": {
      "type": "builtin",
      "enabled": true,
      "_description": "Platform built-in web fetch for URL reference analysis"
    },
    "storybook": {
      "type": "remote",
      "url": "http://localhost:6006/mcp",
      "enabled": true,
      "_description": "Cortex Design Library — component docs, props, and stories via Storybook MCP. Available when `npm run storybook` is running."
    }
  },
  "agent": {
    "design-agent-lead": {
      "description": "Lead design agent — conversational design direction + visual token emission (11 CSS custom properties)",
      "mode": "subagent"
    },
    "component-adapter": {
      "description": "Component catalog query + token adaptation engine — deterministic, reads .storybook/component-catalog.json",
      "mode": "subagent"
    }
  },
  "plugin": [
    "/home/stefan/Cortex/.opencode/plugins/graphify.js"
  ]
}
